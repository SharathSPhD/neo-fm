# neo-fm vocal-synth container — Phase 7 stub.
#
# Phase 7: this becomes the svara-TTS Indic singing voice container that takes
#          melody + phonemes (from Song Document) and produces vocal stems that
#          get mixed onto the HeartMuLa instrumental.
#
# Phase 0: do-nothing placeholder so the docker-compose service exists and the
#          directory is real in the repo.

FROM python:3.12-slim

WORKDIR /app
COPY README.md ./

CMD ["python", "-c", "print('vocal-synth: Phase 7 stub. See README.md.'); import time; time.sleep(3600)"]
